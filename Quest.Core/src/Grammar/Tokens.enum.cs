namespace Quest.Core.Grammar
{
    public enum Tokens
    {
        OR_ELSE,
        AND_ALSO,
        EQUAL,
        NOT_EQUAL,
        LOWER_THAN,
        GREATER_THAN,
        LOWER_THAN_EQUAL,
        GREATER_THAN_EQUAL,
        PATTERN,
        IN,
        NOT_IN
    }
}